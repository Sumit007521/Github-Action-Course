name: 01 - Building Blocks
on: 
  - push
  - workflow_dispatch

jobs:
  name: echo_hello
  runs-on: ubuntu-latest
  steps:
    - name: 'Say Hello'
      run: echo "Hello, World!"

  # name: echo-goodbye
  # runs-on: ubuntu-latest
  # steps:
  #   - name: 'Failed step'
  #     run: |
  #       echo "I will fail"

  #   - name: 'Say goodbye'
  #     run: echo "Goodbye!"
