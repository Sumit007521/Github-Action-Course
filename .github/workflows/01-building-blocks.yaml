name: 01 - Building Blocks
on: 
  - push
  - workflow_dispatch

job1:
  name: echo-hello
  runs-on: ubuntu-lates
  steps:
    - name: 'Say Hello'
      run: echo "Hello, World!"

job2:
  name: echo-goodbye
  runs-on: ubuntu-latest
  steps:
    - name: 'Failed step'
      run: |
        echo "I will fail"

    - name: 'Say goodbye'
      run: echo "Goodbye!"
